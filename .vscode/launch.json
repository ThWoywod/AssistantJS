{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "ALL - Specs",
      "program": "${workspaceRoot}/node_modules/.bin/lerna",
      "args": ["exec", "gulp" , "build-local"],
      "stopOnEntry": false,
      "cwd": "${workspaceRoot}",
      "protocol": "inspector",
      "outputCapture":"std"
    }
  ]
}